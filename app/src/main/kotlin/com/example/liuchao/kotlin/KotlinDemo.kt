package com.example.liuchao.kotlin

/**
 * Created by yangrenije on 2017/9/4.
 */
//object SingleTon

var obj = object { }


//fun main(args: Array<String>) {
//    println(SingleTon)
//}

/*
fun print() {
    var d: String? = null
    print(d?.length)
    var list = mutableListOf("kotlin", "java", "c", "c++", "php", "python", "ruby", "javascript")
    list.sortByDescending { it.length }
//    var groups = list.groupBy { if (it.startsWith("j")) "java" else "other" }
    list.remove("kotlin")
    list.forEachIndexed { index, s -> println("${s}") }//.map { "language: ".plus(it) }.mapIndexed { index, s -> index.toString().plus(".").plus(s) }
}*/
